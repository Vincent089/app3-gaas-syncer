apiVersion: v1
kind: Service
metadata:
  name: gaas-syncer
  namespace: app3
  labels:
    app: gaas-syncer
spec:
  selector:
    app: gaas-syncer
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
